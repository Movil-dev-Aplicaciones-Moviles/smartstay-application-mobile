package com.smartstay.application_mobile_frontend.feature.iam.presentation.navigation

/**
 * Centralized routing dictionary for the application navigation graph.
 *
 * All route strings are defined here as constants so composables and resolvers
 * never hold hardcoded strings — any rename is a single-point change.
 */
object AppRoutes {
    const val SPLASH               = "splash"
    const val SIGN_IN              = "sign_in"
    const val SIGN_UP              = "sign_up"
}